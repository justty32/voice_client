# specs — 設計方案

spec 回答「要做什麼、為何這樣做、有哪些取捨」，不展開成逐行施工單。

規劃階梯：`idea → roadmap → spec → plan → feature-dev`。

現役設計放在本資料夾，命名 `<feature>-design.md`；已落地或被取代後移入
[archive/](archive/README.md)。設計涉及資料流時要引用 topic／payload 協定與 CODE_MAP。

目前沒有現役 spec。
